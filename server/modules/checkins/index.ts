import { reservedModule } from "../_shared/module";

export const checkinsModule = reservedModule(
  "checkins",
  "Check-in source snapshots, attendance projections and anomaly detection inputs",
);
