import { reservedModule } from "../_shared/module";

export const announcementsModule = reservedModule(
  "announcements",
  "Announcements, required acknowledgements, supervisor publishing and read-status overview",
);
