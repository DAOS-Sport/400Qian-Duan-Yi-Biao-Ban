import { reservedModule } from "../_shared/module";

export const schedulesModule = reservedModule(
  "schedules",
  "Schedule source snapshots, shift reminder projections and schedule adapter orchestration",
);
