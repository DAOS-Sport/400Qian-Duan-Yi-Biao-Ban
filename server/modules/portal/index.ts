import { reservedModule } from "../_shared/module";

export const portalModule = reservedModule(
  "portal",
  "Legacy portal migration boundary; existing portal routes move here only when touched",
);
