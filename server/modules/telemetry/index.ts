import { reservedModule } from "../_shared/module";

export const telemetryModule = reservedModule(
  "telemetry",
  "UI events, client errors, audit writer, request correlation and telemetry rate limiting",
);
