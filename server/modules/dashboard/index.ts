import { reservedModule } from "../_shared/module";

export const dashboardModule = reservedModule(
  "dashboard",
  "Projection-backed dashboard summaries and role-specific overview orchestration",
);
