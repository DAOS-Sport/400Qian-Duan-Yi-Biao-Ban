import { reservedModule } from "../_shared/module";

export const tasksModule = reservedModule(
  "tasks",
  "Task assignment, task status state machine, employee reporting and supervisor completion flow",
);
