import { reservedModule } from "../_shared/module";

export const systemModule = reservedModule(
  "system",
  "System health, integration overview, raw inspector, admin-only governance and audit drill-down",
);
