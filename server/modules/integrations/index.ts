import { reservedModule } from "../_shared/module";

export const integrationsModule = reservedModule(
  "integrations",
  "Integration job orchestration, source snapshots, adapter health and reconnect status",
);
