import { reservedModule } from "../_shared/module";

export const anomaliesModule = reservedModule(
  "anomalies",
  "Clock-in anomaly reporting, employee explanation, supervisor review and resolution audit",
);
